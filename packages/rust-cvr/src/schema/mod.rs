//! Port of the `packages/zero-cache/src/services/view-syncer/schema/` directory:
//! the CVR Postgres schema (`schema/cvr.ts`) and the shared CVR type/version
//! system (`schema/types.ts`).

pub mod cvr;
pub mod types;
